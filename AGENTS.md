# 项目开发规范

本文件适用于当前仓库及其所有子目录。

## 默认策略

默认不新增单元测试和项目文档。用户明确要求、仓库规则强制要求，或缺少测试会使高风险改动无法可靠验证时，再新增必要的测试或文档。

错误捕获边界、并发调度、检查点恢复、持久化格式、输出路径查找、子进程协议和资源生命周期属于高风险行为。修改这些行为时，如果现有测试无法验证关键约束，应新增最小范围的回归测试，不受“默认不新增单元测试”限制。

完成代码修改后，优先运行仓库已有的格式化、静态检查、类型检查和与本次改动相关的现有测试。无法运行时，应说明原因和未验证的部分。

会修改文件的格式化和自动修复工具，优先只对本次任务涉及的文件运行。全仓库检查应优先使用不修改文件的检查模式；确需执行全仓库格式化或自动修复时，应先取得用户确认。

## 工作范围

只完成用户直接要求以及完成该要求必需的修改。

不要添加为未来需求准备的配置、抽象、兼容层、扩展点或辅助函数。不要在修复问题时顺便调整无关文件、命名、目录结构或代码风格。

修改前应阅读相关实现、数据结构、配置和调用方，理解数据怎样进入程序、经过哪些处理以及最终输出到哪里。优先沿用项目现有的目录结构、命名方式和技术方案。

发现用户方案存在明显的维护成本、兼容风险、安全问题或重复建设时，应说明具体影响并给出推荐方案。涉及扩大修改范围、改变公开行为、修改数据格式或删除现有能力时，应先取得用户确认。

## 代码架构和质量

使用符合当前语言和框架习惯的代码结构。代码应保持职责清楚、依赖简单、容易阅读和修改。

### 避免重复

具有业务含义、需要多处共享或必须保持一致的文本、数值、路径、状态值和配置项，应定义为语义明确的常量，并放在离使用位置最近的合理层级。

一次性使用、含义清楚的普通字面量无需提取为常量。避免为了消除表面重复而创建大量只使用一次的常量。

相同业务规则出现多次时，先确认这些代码是否必须保持一致。确认属于同一规则后，优先提取函数、模块或组合对象；只是结构相似但含义或变化原因不同的代码，不要强行共用抽象。只有存在真实的多种实现、替换需求或稳定边界时，才引入接口或基类。

### 模块职责

按领域或功能组织代码，使业务处理、数据访问、配置和通用工具分别承担明确职责。

入口文件负责解析输入、装配依赖、组织执行顺序和返回结果。复杂算法、大型配置映射、协议处理和具体数据转换应放入职责明确的子模块。

相关数据结构和处理这些数据的代码应尽量放在相近位置。模块之间使用清晰、精简的接口交互，避免循环依赖和跨层访问内部实现。

一个文件包含多个互不相关的职责时，应按职责拆分。文件达到 300—500 行时，应检查它是否承担了过多职责。行数只是检查信号，拆分决定应以职责是否清楚为准。

拆分后的模块应具备完整、明确的用途。不要为了减少文件行数而创建大量内容零散、需要频繁跳转的小文件。

同一类输出路径的目录名和构造规则应集中定义。路径构造函数只负责计算路径；目录创建、文件写入和兼容目录查找由实际拥有这些行为的模块负责。

### 函数设计

函数应只完成一个可以清楚命名的任务。

函数超过约 50 行、包含多个处理阶段或需要较长说明时，应检查能否提取具有独立含义的子函数。连续、简单且高度相关的代码可以保留在同一个函数中。

嵌套层级原则上不超过 3 层。优先使用卫语句处理异常条件和提前返回。复杂循环、条件分支和数据转换应提取为名称清楚的函数。

没有独立业务含义，只负责原样传递参数和返回值，并且不执行数据转换、校验、错误转换、资源管理或协议适配的函数，应直接内联。

函数即使只有一次调用，只要它负责命名一个完整业务步骤、维护公开接口、隔离外部依赖或统一资源生命周期，也可以保留。删除函数前应检查导出接口和仓库外可能存在的调用方。

### 配置和运行时装配

配置对象应表示会被同一个组件整体使用的一组配置。不要仅为了减少参数数量、转发字段或包装另一个配置对象而新增配置类。

环境变量、配置文件等外部输入应在程序边界集中解析。完成解析后，应尽早构造可以直接工作的客户端或运行时对象。业务流程接收已经准备好的依赖，避免多个调用层反复拆开、转发和重新包装相同字段。

多个字段只有在生命周期一致、总是共同使用，并且能够形成明确概念时，才组合为运行时对象。不要把互不相关的依赖放入通用上下文对象。

### 抽象边界

抽象应服务于已经存在的重复、明确的职责边界或真实的替换需求。

只有一个实现并且没有替换需求时，可以直接使用具体类型。存在多个实现、需要隔离外部系统或调用方依赖稳定协议时，可以使用接口、协议或基类。

优先使用组合组织功能。继承只用于存在明确且稳定的类型关系，并且子类能够完整遵守父类约定的场景。

### 数据和状态

为重要数据结构使用含义清楚的类型和字段名。避免使用无法表达内容的通用名称，例如 `data`、`item`、`result` 或 `temp`；局部范围很小且含义明显时可以使用。

明确区分原始输入、处理中间结果和最终输出。不要让同一个变量在不同阶段代表不同类型或不同含义的数据。

需要跨多个处理阶段传递、存在成功和失败分支，或会被多个模块读取的业务结果，应优先使用 `dataclass`、`TypedDict` 或其他明确类型。不要让无结构字典成为内部流水线的主要数据契约。

字典适合外部接口载荷、动态字段和最终序列化。业务对象应尽量在文件写入、JSON 输出或网络请求边界统一转换为字典，避免处理中途反复添加和读取字符串键。修改内部类型时，默认保持最终序列化字段及其含义不变。

共享状态应尽量减少。能够作为参数传递或由函数返回的数据，不要放入可变的全局状态。

修改数据时，应明确哪些字段发生变化，哪些字段保持不变。筛选、排序、分组、复制和包装数据时，不要使用会让读者误以为原始值已经改变的命名。

### 验证和错误处理

信任语言、框架和内部模块已经明确保证的条件。输入校验集中在用户输入、外部接口、文件、网络、数据库和其他系统边界。

只处理当前需求中可能实际发生，并且程序能够采取明确措施的错误。避免为理论场景增加复杂的兜底分支。

预期会由程序直接展示或恢复的错误，应转换为项目定义的领域异常。错误类型应表达调用方如何处理它，例如命令是否停止、批量任务是否只跳过当前对象。

命令入口只捕获可以直接展示给用户的项目异常；批量处理边界只捕获允许跳过当前对象的异常。未预期的异常应继续传播并保留 traceback，不要因为宽泛捕获 `Exception`、`ValueError` 或 `OSError` 而把程序错误误报成普通业务失败。

只有在线程、子进程、协议响应或清理资源等必须阻止异常越过边界的场景，才捕获 `Exception`。捕获后必须记录 traceback，并明确执行重新抛出、异常转换或协议返回。

错误信息应说明失败的操作和定位问题所需的上下文，同时避免泄露密钥、令牌、密码和个人信息。

捕获异常后，应执行明确的恢复、转换、记录或清理操作。没有处理能力时，让异常继续向上传递，避免静默忽略错误。

资源应在成功、失败和提前返回时都能正确释放。涉及文件、网络连接、数据库事务、锁或后台任务时，优先使用语言或框架提供的自动清理机制。

### 防止过度设计

遵循 YAGNI 原则，只实现当前需求需要的结构。

禁止添加“未来可能需要”的配置、接口、兼容层、扩展点或辅助函数。

禁止在修复问题时顺便清理周围无关的代码风格、命名或目录结构。

禁止为没有实际发生路径的场景添加复杂错误处理。

禁止为了套用设计模式而增加额外层级。简单、直接且容易维护的实现优先。

### 向后兼容和兜底

新增功能无需主动设计历史版本兼容层、多套实现或降级路径。

修改已有代码时，默认保留现有公开接口、接口字段、命令行参数、数据格式、持久化格式和调用方可以观察到的行为。

用户明确要求破坏性修改时，可以直接按新设计实施。保留旧行为会显著妨碍当前实现时，应说明受影响的调用方、数据和使用方式，并在实施前取得用户确认。

不要保留已经确认没有调用方的旧实现。不要添加用户未要求、也没有明确使用场景的兜底行为。

### 保持行为的重构

仅调整代码结构的重构，默认不得改变以下行为：

- 公开函数、参数、默认值和返回类型；
- 校验发生的时机、错误传播范围和批量任务是否继续；
- JSON 字段、检查点内容、输出路径和恢复任务的查找方式；
- 并发上限、任务顺序、取消行为和资源关闭时机。

修改前应确认这些行为由哪些调用方依赖。修改后应分别检查正常路径、预期失败路径和未预期异常路径，不能只确认最终成功输出。

### 中文字符串和日志

面向用户、开发者或运维人员的中文字符串使用简体中文，并优先描述具体、可以观察到的行为。相同概念在提示文本、日志和错误信息中使用同一种说法，避免读者把不同称呼误认为不同功能。

日志应让不了解代码的读者看懂当前发生了什么。重要日志至少说明执行的操作、涉及的对象、执行结果，以及失败或跳过的原因。日志描述处理过程和结果，不复述“调用函数”“设置变量”这类代码动作。

日志级别应符合事件影响：正常流程使用信息级别，可恢复但需要关注的情况使用警告级别，操作失败或无法继续时使用错误级别。简单赋值、普通循环和不会影响判断的中间步骤无需打印日志。

日志应包含定位问题所需的稳定标识，例如任务编号、文件名或请求编号。禁止记录密码、令牌、完整身份信息和其他敏感内容。能够使用结构化字段时，优先将标识和数值作为字段传入，避免依赖难以检索的长文本拼接。

## 修改和验证流程

执行代码修改时：

1. 阅读与需求直接相关的代码、配置、数据结构和调用方。
2. 说明准备修改的行为和可能影响的范围。
3. 使用满足需求的最小改动完成实现。
4. 检查是否引入重复逻辑、职责混杂、无意义抽象或无关改动。
5. 对本次任务涉及的文件运行格式化和自动修复，并运行仓库已有的静态检查、类型检查和相关测试。
6. 检查实际代码差异，确认没有修改无关文件。
7. 向用户说明完成内容、验证结果以及仍然存在的风险。

默认无需为简单任务单独编写计划。涉及多个文件、多个阶段或存在明显风险时，可以先给出简短计划。
